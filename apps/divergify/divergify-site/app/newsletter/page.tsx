'use client'; import {useState} from 'react'; export default function News(){ const [email,setEmail]=useState(''); const [msg,setMsg]=useState(''); const submit=async()=>{ const r=await fetch('/api/newsletter',{method:'POST',body:JSON.stringify({email})}); setMsg(r.ok?'You’re on the list.':'Try again.'); setEmail(''); }; return (<div><h1>What’s Next</h1><div><input value={email} onChange={e=>setEmail(e.target.value)} placeholder='you@proton.me'/><button onClick={submit}>Sign up</button></div><p>{msg}</p></div>); }